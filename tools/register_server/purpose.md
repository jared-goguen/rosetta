Register or update an external MCP server in the catalog

Manually register or update an external MCP server in Rosetta's server catalog. This creates or updates metadata that agents can use to discover and compose tools across multiple servers. The server information is persisted as a YAML file and included in the schema bundle.
