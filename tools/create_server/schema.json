{
  "input": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Name of the MCP server project"
      },
      "directory": {
        "type": "string",
        "description": "Parent directory where the project will be created"
      },
      "dependencies": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Optional additional dependencies to include in package.json"
      },
      "tools": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Optional list of initial tool names to create empty tool directories for"
      }
    },
    "required": ["name", "directory"],
    "additionalProperties": false
  },
  "output": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Full path to the created project directory"
      },
      "files": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "List of files that were created"
      }
    },
    "required": ["path", "files"],
    "additionalProperties": false
  }
}