{
  "input": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Name of the tool to update"
      },
      "content": {
        "type": "string",
        "description": "New TypeScript source code for index.ts"
      },
      "description": {
        "type": "string",
        "description": "New description for purpose.md"
      },
      "schema": {
        "type": "object",
        "properties": {
          "input": {
            "type": "object",
            "description": "Updated input schema (merged with existing)"
          },
          "output": {
            "type": "object", 
            "description": "Updated output schema (merged with existing)"
          }
        },
        "additionalProperties": false
      }
    },
    "required": ["name"],
    "additionalProperties": false
  },
  "output": {
    "type": "object",
    "properties": {
      "success": {
        "type": "boolean",
        "enum": [true]
      }
    },
    "required": ["success"]
  }
}